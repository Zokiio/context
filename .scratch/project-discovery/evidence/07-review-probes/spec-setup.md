Spec review of PR #9 setup and PR #10 documentation/trial at `2777f087a5e320594e09c5cda1f35d62e66c5a80`. Reviewed the pinned whole stack and the individual setup/docs slices. The review is not clean: two P2 findings in PR #9. No additional Spec finding in PR #10.

1. **P2: Coordinate shared and personal setup for the same directory.** `internal/discovery/setup_write.go:120` locks only the destination file. Shared and personal registrations use different locks, so two cooperating writers can both pass the final resolution check and commit conflicting declarations. Independent same-process and two-process probes synchronized at the existing rename seam; both writes succeeded, then discovery failed. Trigger: same canonical binding directory and records, but different allowed roots. Spec line 92: "If a conflicting declaration would remain in another file, fail without writing and identify the declaration requiring repair." This finding concerns equal-depth bindings; ancestor/descendant bindings are intentionally allowed.

2. **P2: Preserve permission ownership and ACLs during replacement.** `internal/discovery/setup_write.go:140-145` copies only mode bits onto the new inode. A configuration with mode `0640` and group `staff` became group `wheel`, changing who can read it. A separate macOS probe showed an explicit `everyone deny write` ACL disappeared after successful replacement. Spec line 86: "Preserve existing file permissions and report failures without leaving truncated files."

The rebuilt reader supplied both tickets' full task context using explicit `--bundle`, repository `--allow-source`, 500 files, and 8388608 bytes; both reports were complete without diagnostics. Existing setup/input tests passed under `-race`. All 37 trial scenarios passed again on a disposable export using the verified retained binary. Independent CLI probes also passed for invalid flags, dry-run/no-op immutability, alias/key retention, unrelated alias values, YAML tags, exact Markdown body bytes, mode bits, and removed-source authorization.

Original probes, exact commands, output, source revision, and source hashes are retained in `/private/tmp/ctx-spec-setup.ipgPU4/`: `setup-race-original-evidence.json`, `setup-race-crossprocess-evidence.json`, `setup-acl-original-evidence.json`, `setup-group-original-evidence.json`, `preservation-evidence.json`, and `independent-trial-result.json`. The tracked worktree remains clean at the pin.
