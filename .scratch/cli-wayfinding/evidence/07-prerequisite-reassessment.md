# Prerequisite reassessment for the final reference

Actor: Codex /root. Date: 2026-09-19. Final source revision: `bba08b9b3bebdfad55c1ac794fd7fd186ea97a28`.

The final user documentation changes the linked reader and discovery references. A complete orientation before reassessment reported exactly those two changed snapshots for each of tickets 01 through 06. Their previous Acceptance records and evidence remain unchanged. This reassessment preserves those historical results and updates the requirement snapshots after reviewing the documentation against the implemented behavior.

The [independent documentation review](07-documentation-review.md) found no contract mismatch. The [final checks](07-final-checks.json) record passing `go test ./...`, `go test -race ./...`, `go vet ./...`, and `go build ./...` against the final source identity. The reader binary used for reassessment was rebuilt from the same revision. No Go source changed after the standalone compatibility correction at `3f96692`.

## Effect on the six slices

1. Shared capture: the references now describe manifest-first shared capture, physical-source reuse and distinct current/cache limits. They do not alter standalone reader contracts. Retained focused tests, seven exact baseline/current CLI comparisons, the compatibility correction, and the full final suites still cover every criterion. The [historical link correction](01-limit-compatibility-link-correction.md) identifies the original shared-capture evidence without modifying the earlier evidence document.
2. Compact orientation: documentation now matches the implemented compact default, full detail, full JSON, grouping, order, and visible partial results. The original compact tests and order correction remain applicable. Resume pointers now name an implemented command.
3. Scoped resumption: the reference documents current selector, checkout, authorization, identity, limits, absent-cache and exit behavior. Its intermediate nonempty-cache rejection was deliberately removed by the approved later checkpoint slices; historical verification of that stage remains preserved. Final tests cover the assembled behavior.
4. Retained checkpoint: provenance, validation, current authorization, root relocation, nullable differences and partial comparisons match the final contract. The earlier timestamp and trimmed-root fixes remain tested. Its intermediate single-note restriction was deliberately removed by ticket 05; it is not a final user limitation.
5. Competing checkpoints: the reference describes the implemented complete predecessor graph, lexical leaves, conflicts, candidate-only snapshot loading, quarantine exclusion and bounded inspection. The retained real graph/checkpoint probes and final Go/race suites still apply.
6. Recovery skills: the public guide links the exact authoring profile and cooperative stopped-writer recovery procedure. The instructions themselves are unchanged since their accepted correction. Retained filesystem trials cover publication, collisions, reconciliation, refusal for unknown/live writers, quarantine and whole-history reset. The actual fresh documentation session also followed the instructions and retained its observed baseline; its final combined assessment belongs to ticket 07.

## Decision boundary

The six prerequisite records are reassessed in dependency order. Each new Acceptance snapshots the current eight directly selected requirement sources, preserves the earlier evidence and decision, adds this rationale and the final checks, and is checked for valid, fresh acceptance. The final ticket remains in progress until its separate combined evidence and remaining verification findings are settled. These are workflow decisions, with no human approval asserted.
