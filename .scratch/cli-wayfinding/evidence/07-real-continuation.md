# Real task continuation trial

Actor: Codex /root observing two independent sessions on 2026-09-19. Base source revision: `afad9950e20cf6b69824ad7c02dfbc73414c51b1`. Final documentation: `bba08b9b3bebdfad55c1ac794fd7fd186ea97a28`.

The task was real unfinished ticket 07 documentation. The first session used GPT-5.6 Terra with high reasoning and no inherited chat. It drafted docs/resuming-work.md, verified an actual repository command, retained the exact governing context, published checkpoint `bf12cd3e-f472-4e62-8acf-1f0d29694e68`, and stopped. Integration into the README and reader/discovery references and further checks remained undone. The root observed the session as completed while the parent ticket remained in progress.

The root then clarified the existing fresh-session criterion in the ticket, without committing it: the successor must have no inherited chat and receive only the repository path, ticket reference, and instruction to continue. The [before and after ticket copies](07-real-continuation/trial-start.json) preserve this change and their digests.

The successor used GPT-6 Astra with high reasoning and `fork_turns: none`. Its [entire prompt and first report](07-real-continuation/successor-initial-report.md) show that no checkpoint ID, previous-session summary, changed-source hint, or answer was supplied before the recovery observation. The agent found the checkpoint via repository instructions, captured current context, inspected default-limit diagnostics, used justified explicit limits, and detected the uncommitted criterion change. It identified completed draft work, remaining integration and checks, and root-owned verification/acceptance before continuing the agreed documentation work. No user reconstruction or fresh approval was requested.

The successor finished four documentation files, ran seven working command forms and two invalid forms with the expected exits, checked 27 local links, and published `4b95850a-e0ec-46bb-abba-8ba1b0ae40b1` with the first observation as predecessor. Its [note](07-real-continuation/successor/successor-note.md) preserves the original work baseline rather than replacing it with unseen final documentation. The [post-publication result](07-real-continuation/successor/post-publication-summary.json) and retained raw JSON show that the successor is the sole candidate, its snapshot is valid, and comparison is complete. The reader correctly exposed the stale prerequisite requirements caused by the new documentation.

The root independently checked every artifact digest, the old-to-new candidate progression, complete comparisons, and actual changed ticket text in the initial successor report. The [manifest](07-real-continuation-manifest.json) identifies all 36 retained artifacts. Original temporary paths and checkout provenance remain unchanged in the copies. The successor source digest manifest matches the committed final documentation and Go sources.

This was a deliberate session stop after meaningful unfinished work, not a process crash. It demonstrates one observed fresh-agent continuation. It does not establish universal model understanding, exclusive ownership, or recovery from every failure. Controlled filesystem interruption, competing publications, unknown/live writers, and non-Git arrangements have separate evidence. The real documentation task involved no device deployment or runtime test, and neither session claimed those checks passed.
