# Work relationships in Markdown sections

Record spec, blocker, and context relationships as Markdown file links in named body sections. The reader derives each relationship's kind from its section. Keeping the authoritative links in the body makes them visible to generic OKF graph tools and gives each relationship one editable home.
